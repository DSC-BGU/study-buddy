import 'package:flutter/material.dart';

class MallScreen extends StatelessWidget {
  MallScreen();
  static const routhName = '/mall';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
